# AGENTS.md

## 适用范围

本文件适用于 `coolapk-desktop` 仓库及其子目录。

## 版本更新规则

- 更新版本号时，必须使用仓库内的版本脚本，不要手工分别修改各个配置文件。
- 从仓库根目录执行：`npm run version:set -- <完整版本号>`。
- 该命令会调用 `scripts/set-version.mjs`，并同步以下文件：
  - `package.json`
  - `package-lock.json`
  - `src/constants/version.ts`
  - `src-tauri/tauri.conf.json`
  - `src-tauri/Cargo.toml`
  - `src-tauri/Cargo.lock`
- 先读取当前版本号，再计算目标版本号；不要把 `+0.1` 或 `+0.0.1` 直接传给版本脚本。脚本参数必须是完整的 `X.Y.Z` 版本号。
- 每次更新版本号时，必须同步更新 Git tag；目标版本 `X.Y.Z` 对应标签必须是 `vX.Y.Z`，不能只修改文件版本号而遗漏标签。
- `scripts/set-version.mjs` 只负责同步文件版本，不会自动创建、移动或推送 Git tag；tag 必须按发布顺序单独处理。

版本增量含义：

- 用户说 `+0.1` 或 `+0.1.0`：递增次版本号，并将补丁号归零。例如 `1.7.3` 更新为 `1.8.0`。
- 用户说 `+0.0.1`：递增补丁版本号。例如 `1.7.3` 更新为 `1.7.4`。

示例：

```powershell
npm run version:set -- 1.8.0
```

## 版本发布顺序

版本发布必须按以下顺序执行：更新文件 → 测试和检查 → commit → 创建 tag → 上传云端。

1. 更新前执行 `git status --short --branch`，保留用户已有的未提交修改，不得使用重置或覆盖操作；只提交本次版本更新涉及的文件。
2. 根据当前版本计算目标版本，并执行 `npm run version:set -- <完整版本号>`。
3. 运行 `npm run typecheck`、`npm test`、`npm run build` 和 `cargo check --manifest-path .\src-tauri\Cargo.toml`，再执行 `git diff --check`。
4. 如果任一测试或检查失败、未通过或无法运行，必须停止后续流程，提醒用户并说明命令和结果；不得继续 commit、创建 tag 或上传。
5. 确认六个版本文件的版本号一致后，创建中文提交：`git commit -m "发布版本 vX.Y.Z"`。
6. 确认本地和远端不存在目标 tag 后，创建带注释 tag：`git tag -a vX.Y.Z -m "发布版本 vX.Y.Z"`。
7. 只有用户明确要求上传、推送或上云时，才推送当前分支和 `vX.Y.Z` tag，并用 `git ls-remote` 验证远端引用。

## 发布提交和标签

- 所有 commit 提交信息使用中文，避免使用全英文提交标题。
- 版本更新完成后，确认所有版本文件使用同一个完整版本号。
- 发布提交格式固定为：`发布版本 vX.Y.Z`。
- 发布标签使用带注释标签：`vX.Y.Z`。
- 发布前确认目标 `vX.Y.Z` 标签指向本次版本提交，分支和标签中的版本号保持一致。
- 如果目标 tag 已存在于本地或远端，禁止强制移动、删除或覆盖，必须先提醒用户处理。
- 普通代码或文档提交不自动创建版本 tag；只有版本发布才创建对应的 `vX.Y.Z` tag。
- 只有用户明确要求上传、推送或上云时，才执行远端操作。
- 上传时推送当前分支和对应版本标签，并使用 `git ls-remote` 验证远端分支和标签已指向预期提交。
- 提交到云端前必须先运行与本次改动相关的测试和检查，所有必需测试通过后才能上传。
- 如果测试失败、未通过或无法运行，必须先提醒用户，说明测试命令和结果，不得继续上传。

## 修改后的检查

- 至少检查 `git diff --check`。
- 版本更新后检查六个版本文件中的版本号是否一致。
- 任何要提交到云端的改动都必须执行版本发布顺序中的测试和检查；仅在不上传云端时，才可以根据改动范围决定是否省略项目测试。
